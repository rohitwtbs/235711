import json
class Car:
    def _init(self,seats,engine,drive,color):
        seld.seats = seats
        self.engine = engine
        self.drive = drive
        self.color = color

    def is_petrol():
        if (self.engine == "petrol"):
            return True
    def is_diesel():
        if (self.engine == "diesel"):
            return True
